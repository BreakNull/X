cygwin\home\用户下的.bash_profile文件中添加ndk路径（具体用自己的ndk路径）
export ANDROID_NDK_ROOT=/cygdrive/e/android_root/android-ndk-r6b
export PATH=${PATH}:$ANDROID_NDK_ROOT

环境变量的path中追加：cygwin 和sdk的路径（自己的路径为准）
F:\cygwin\bin;E:\android_root\android-sdk-windows\tools;

